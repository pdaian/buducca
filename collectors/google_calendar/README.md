# Google Calendar collector

Fetches events for each configured account and writes one file per account/month pair:

- `workspace/google_calendar/<account>.<YYYY-MM>.events.jsonl`

Uses Google Agentic CLI command template from config.
