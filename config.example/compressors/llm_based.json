{
  "interval_seconds": 86400,
  "command": "python3 scripts/memory_compressor.py",
  "timeout_seconds": 90,
  "files": [
    {
      "path": "learnings",
      "interval_seconds": 86400,
      "backup_path": "learnings.back",
      "prompt": "Current date/time (UTC, accurate to the minute): {now}\n\nYou are running a memory compressor for {file_path}. Remove redundant information while preserving all important, non-overlapping details. Return JSON with keys \"compressed_content\" and \"removed_content\"."
    }
  ]
}
