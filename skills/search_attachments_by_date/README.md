# Search attachments by date skill

Lists files stored under `attachments/` for a specific date folder.

## Example

```bash
python3 -m assistant_framework.cli skill search_attachments_by_date --args '{"date":"2026-03-10","max_items":20}'
```
